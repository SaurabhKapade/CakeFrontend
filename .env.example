# Same env name as Pizza-Frontend (defaults to http://localhost:3000)
# VITE_BACKEND_URL=http://127.0.0.1:3000

# Optional: shop contact (not used for ordering — orders are tracked in-app)
# VITE_SHOP_PHONE=918208497945
